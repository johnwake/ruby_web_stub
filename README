Welcome, 

This is a simple Sinatra Web stub that will allow you to customise or use the default views set for mocking Web services for Automated tests. 

You can install all the Gems but running 
$ bundle install

To start the web service type the following into the command line
$ shotgun